{
  // Tests and scripts may use Node APIs. The simulation itself may not —
  // see sim/tsconfig.json. `npm run typecheck` checks both projects.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["scenarios", "scripts", "tests"]
}
